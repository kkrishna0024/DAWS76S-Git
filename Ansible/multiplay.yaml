- name: playbook
  hosts: web
  becomes: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start service Nginx, if not started
    ansible.builtin.service:
       name: httpd
       state: started
     